<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to right, #032f3c, #02474d, #046169, #217b7e, #10898d);
        }

        #note-container {
            width: 80%;
            display: flex;
            flex-direction: column;
            align-items: center;
            color: white;
        }

        #note {
            width: 100%;
            height: 80vh;
            border: 1px solid #ccc;
            padding: 10px;
            background-color: white;
            overflow: auto;
            box-sizing: border-box;
        }

        #noteContent {
            width: 100%;
            height: 100%;
            border: none;
            outline: none;
            font-size: 16px;
            color: #333;
            padding-left: 20px;
        }

        #controls {
            margin-top: 10px;
            display: flex;
            justify-content: space-between;
        }

        #saveButton {
            margin-top: 10px;
            padding: 10px 20px;
            background-color: #10898d;
            color: white;
            border: none;
            cursor: pointer;
        }

        #saveButton:hover {
            background-color: #046169;
        }
    </style>
</head>

<body>
    <div id="note-container">
        <h1>YOUR NOTES</h1>
        <div id="note">
            <div id="controls">
                <input type="color" id="colorPicker" title="Text Color">
                <input type="number" id="fontSize" placeholder="Font Size">
                <select id="bulletType">
                    <option value="none">None</option>
                    <option value="disc">Bullet</option>
                    <option value="square">Square</option>
                    <option value="decimal">Numbered</option>
                </select>
            </div>

            <!-- Content editable note -->
            <div id="noteContent" contenteditable="true" placeholder="Start typing..."></div>
        </div>

        <!-- Hidden form to send note content -->
        <form id="noteForm" action="/notes" method="POST">
            <input type="hidden" name="content" id="noteInput">
            <button type="submit" id="saveButton">Save Note</button>
        </form>
    </div>

    <script src="js/notes.js"></script>
</body>

</html>
